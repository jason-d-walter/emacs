;;; package -- Summary
;;;
;;; Commentary:
;;;
;;; Provides settings for cquery
;;;
;;; Code:


(require 'company)
(require 'company-lsp)
(require 'use-package)
;;(require 'cquery)
;;(use-package cquery
;;  :init
;;  (setq cquery-executable (concat (getenv "HOME") "/bin/ntx86/cquery.exe"))
;;  (setq cquery-cache-dir "build/.cquery_cached_index")
;;  :config
;;  (setq cquery-sem-highlight-method 'font-lock)
;;  ;; For rainbow semantic highlighting
;;  (cquery-use-default-rainbow-sem-highlight)
;;  )

(provide 'my-cquery-settings)

;;; my-cquery-settings.el ends here
